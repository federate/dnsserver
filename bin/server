#!/usr/bin/env ruby

require 'bundler/setup'

require File.expand_path('../../lib/dnsserver', __FILE__)

opts = Slop.parse do
  banner 'Usage: server [options]'

  on :c, :config=, 'config file'
end

config = opts[:config] ? opts[:config] : nil

#DNSServer.run

pry.binding
